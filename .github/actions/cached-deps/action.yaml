name: "Cache dependencies"
description: "A simple composite action that prints a greeting"
runs:
  using: "composite"
  steps:
    - name: Cache node modules
      uses: actions/cache@v3
      with:
        path: |
          ~/.npm
          node_modules
        key: ${{ env.CACHE_KEY }}-${{ hashFiles('package-lock.json') }}
    - name: Install dependencies
      run: npm ci
      shell: bash
